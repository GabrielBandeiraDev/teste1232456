# 📦 Instalação de Dependências

## Para importar Excel, você precisa:

```bash
cd backend
source venv/bin/activate  # Ativar o ambiente virtual
pip install openpyxl
```

Ou instalar todas as dependências:

```bash
cd backend
source venv/bin/activate
pip install -r requirements.txt
```

## Depois de instalar, execute:

```bash
python import_excel_to_db_safe.py
```
